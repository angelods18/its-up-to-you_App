import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.itsup2u.app',
  appName: 'itsup2u',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
